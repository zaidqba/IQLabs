[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Pure unit tests (no network, no DB)
    integration: Integration tests (require TEST_DATABASE_URL)
    validation: IQ/OQ/PQ validation scripts
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
